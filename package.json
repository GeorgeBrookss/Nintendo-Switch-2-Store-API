{
  "name": "nintendo-switch-2-store-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "json-server --watch db.json --host 0.0.0.0 --port 3000",
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}